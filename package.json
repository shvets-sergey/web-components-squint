{
  "name": "web-components-squint",
  "version": "0.0.1",
  "description": "Demo of web components with squint",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --root-dir=public --open --watch",
    "deploy-local": "rm -rf public/js/* && mv src/*.mjs public/js/",
    "compile": "./node_modules/squint-cljs/node_cli.js compile src/index.cljs"
  },
  "author": "Sergey Shvets",
  "license": "EPL-1.0",
  "dependencies": {
    "lit": "^2.7.6"
  },
  "devDependencies": {
    "@web/dev-server": "^0.3.0",
    "express": "^4.18.2"
  }
  
}
